# Portable Runtime Loader

Load the repository root `SKILL.md` as the authoritative instructions. For pricing requests, read `references/pricing-guide.md` and use `scripts/run_pricing.py`; report assumptions, validation flags, and the research-only disclaimer. Compatible with Hermes and OpenClaw runtimes.
